//! Dioxus-based landscape components (replacing the hand-built string
//! templating in `src/view/html.rs`). Red-phase scaffolding: `matrix` carries
//! the pure helpers moved verbatim from `html.rs`, `defense` is stub-only.

pub mod matrix;
pub mod defense;
