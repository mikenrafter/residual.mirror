<task-notification>
<task-id>ae697a3da1ccac234</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1000/-home-v0id-Documents-repos-residual/0377c2d5-06de-4af8-99b6-293e1014da2c/tasks/ae697a3da1ccac234.output</output-file>
<status>completed</status>
<summary>Agent "Red phase: state-mutation action tests" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>All 75 pre-existing tests pass unchanged, and the 17 new failing tests fail via the intended stub `Error("not yet implemented — green phase")` throws (verified in the tail output above), not compile errors. 3 duplicate-rejection tests (`addComponentColumn`/`addAttractorOption` throw tests) pass coincidentally since the stub already throws — that's expected and correct for red phase.

## Summary

Files created:
- `/home/v0id/Documents/repos/residual/web/src/actions.ts` — stub reducer module
- `/home/v0id/Documents/repos/residual/web/src/actions.test.ts` — 20 tests (17 red/fail-on-stub, 3 already-pass duplicate-throw tests)

Function signatures:
```ts
function toggleComponent(state: PendingState, forceKey: string, componentName: string): PendingState
function addForceRow(state: PendingState, kind: "stressor" | "purpose"): { state: PendingState; tempId: string }
function updateForceField(
  state: PendingState, forceKey: string,
  field: "description" | "attractorId" | "naiveChangeOrFeature" | "outcomes" | "shortname",
  value: string,
): PendingState
function addComponentColumn(state: PendingState, component: SnapshotComponent): PendingState
function addAttractorOption(state: PendingState, attractor: SnapshotAttractor): PendingState
```

Pinned behavior for the base-force toggle-back question: toggling a component OFF that restores a base force's `updatedForces[id].components` to exactly match the base force's original `components` list **prunes the `components` key out of the update entry** (or deletes the whole entry if `components` was the only key set) rather than leaving a no-op array sitting in state. This keeps state minimal and avoids a spurious update line downstream in `model.ts`'s `toCommandLines`. If other fields (e.g. `description`) are already set on that update entry, only `components` is pruned — the entry itself survives. This is documented as a code comment at the top of `actions.ts` and pinned by three tests: "toggling a component OFF for a base force that restores the original list prunes the updatedForces entry entirely", "toggling OFF a restoring component preserves other already-set fields on the update entry, pruning only components", and the seed/add-on-first-toggle and remove-from-original-list cases.

`addForceRow` returns `{ state, tempId }` (not just `state`) so the DOM layer can open the new row in edit mode immediately; tempIds are generated as `"NEW-&lt;n&gt;"` with `n` derived from the max existing `NEW-&lt;number&gt;` tempId + 1 (collision-proof even after a hypothetical future row-removal action).

Test tail output:
```
 79 pass
 17 fail
 413 expect() calls
Ran 96 tests across 5 files. [175.00ms]
```
Pre-existing suite run in isolation confirmed unchanged: `75 pass, 0 fail` across import-parser.test.ts, import-parser.parity.test.ts, model.test.ts, render-decisions.test.ts.</result>
<usage><subagent_tokens>41028</subagent_tokens><tool_uses>6</tool_uses><duration_ms>75551</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>aff16ee99ca016c2b</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1000/-home-v0id-Documents-repos-residual/0377c2d5-06de-4af8-99b6-293e1014da2c/tasks/aff16ee99ca016c2b.output</output-file>
<status>completed</status>
<summary>Agent "Green phase: implement state-mutation actions" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>All 96 tests pass, 0 failures — the pre-existing 79 (import-parser, model, render-decisions) are still green alongside the 17 newly-passing actions tests.

## Implementation (`/home/v0id/Documents/repos/residual/web/src/actions.ts`)

```ts
// Pure reducer layer for the live-editing DOM to call into: takes a
// PendingState (see model.ts) and a user action's arguments, returns a NEW
// PendingState. No DOM, no mutation of the input state.
// ... (doc comments unchanged from stub) ...

import type { AddedForce, PendingState, SnapshotAttractor, SnapshotComponent } from "./model";

function sameElements(a: readonly string[], b: readonly string[]): boolean {
  if (a.length !== b.length) return false;
  const sortedA = [...a].sort();
  const sortedB = [...b].sort();
  return sortedA.every((value, index) =&gt; value === sortedB[index]);
}

function toggled(components: readonly string[], name: string): string[] {
  return components.includes(name) ? components.filter((c) =&gt; c !== name) : [...components, name];
}

export function toggleComponent(state: PendingState, forceKey: string, componentName: string): PendingState {
  const addedIndex = state.addedForces.findIndex((f) =&gt; f.tempId === forceKey);
  if (addedIndex !== -1) {
    const target = state.addedForces[addedIndex]!;
    const nextForce: AddedForce = { ...target, components: toggled(target.components, componentName) };
    const nextAddedForces = [...state.addedForces];
    nextAddedForces[addedIndex] = nextForce;
    return { ...state, addedForces: nextAddedForces };
  }

  const base = state.baseForces.find((f) =&gt; f.id === forceKey);
  if (base === undefined) return state;

  const existingUpdate = state.updatedForces[forceKey];
  const effective = existingUpdate?.components ?? base.components;
  const nextComponents = toggled(effective, componentName);

  const nextUpdatedForces = { ...state.updatedForces };

  if (sameElements(nextComponents, base.components)) {
    if (existingUpdate === undefined) return state;
    const { components: _components, ...rest } = existingUpdate;
    if (Object.keys(rest).length === 0) {
      delete nextUpdatedForces[forceKey];
    } else {
      nextUpdatedForces[forceKey] = rest;
    }
  } else {
    nextUpdatedForces[forceKey] = { ...existingUpdate, components: nextComponents };
  }

  return { ...state, updatedForces: nextUpdatedForces };
}

export function addForceRow(
  state: PendingState,
  kind: "stressor" | "purpose",
): { state: PendingState; tempId: string } {
  let maxN = 0;
  for (const f of state.addedForces) {
    const match = /^NEW-(\d+)$/.exec(f.tempId);
    if (match) {
      const n = Number(match[1]);
      if (n &gt; maxN) maxN = n;
    }
  }
  const tempId = `NEW-${maxN + 1}`;
  const newForce: AddedForce = {
    tempId, kind, description: "", attractorId: "", naiveChangeOrFeature: "",
    outcomes: "", shortname: "", components: [],
  };
  return { state: { ...state, addedForces: [...state.addedForces, newForce] }, tempId };
}

export function updateForceField(
  state: PendingState,
  forceKey: string,
  field: "description" | "attractorId" | "naiveChangeOrFeature" | "outcomes" | "shortname",
  value: string,
): PendingState {
  const addedIndex = state.addedForces.findIndex((f) =&gt; f.tempId === forceKey);
  if (addedIndex !== -1) {
    const target = state.addedForces[addedIndex]!;
    const nextForce: AddedForce = { ...target, [field]: value };
    const nextAddedForces = [...state.addedForces];
    nextAddedForces[addedIndex] = nextForce;
    return { ...state, addedForces: nextAddedForces };
  }
  const existingUpdate = state.updatedForces[forceKey];
  return { ...state, updatedForces: { ...state.updatedForces, [forceKey]: { ...existingUpdate, [field]: value } } };
}

export function addComponentColumn(state: PendingState, component: SnapshotComponent): PendingState {
  const exists = state.baseComponents.some((c) =&gt; c.name === component.name) ||
    state.addedComponents.some((c) =&gt; c.name === component.name);
  if (exists) throw new Error(`component "${component.name}" already exists`);
  return { ...state, addedComponents: [...state.addedComponents, component] };
}

export function addAttractorOption(state: PendingState, attractor: SnapshotAttractor): PendingState {
  const exists = state.baseAttractors.some((a) =&gt; a.id === attractor.id) ||
    state.addedAttractors.some((a) =&gt; a.id === attractor.id);
  if (exists) throw new Error(`attractor "${attractor.id}" already exists`);
  return { ...state, addedAttractors: [...state.addedAttractors, attractor] };
}
```

## Test tail output

```
src/actions.test.ts:
(pass) toggleComponent &gt; ... (7 tests)
(pass) addForceRow &gt; ... (3 tests)
(pass) updateForceField &gt; ... (5 tests)
(pass) addComponentColumn &gt; ... (3 tests)
(pass) addAttractorOption &gt; ... (3 tests)

 96 pass
 0 fail
 451 expect() calls
Ran 96 tests across 5 files. [153.00ms]
```

## Deviations

None. Test file, `model.ts`, and other files were not touched. `toggleComponent`'s base-force branch compares the pruning condition via set equality (`sameElements`) rather than strict order equality, since `toggled()` naturally reconstructs the original order for the pinned test cases anyway — this is a conservative choice that satisfies "restores its effective list to exactly the base force's original components" semantically without being fragile to incidental ordering.</result>
<usage><subagent_tokens>44023</subagent_tokens><tool_uses>5</tool_uses><duration_ms>57604</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a8317c0f57e4d6c4f</task-id>
<tool-use-id>toolu_01XPfEJkp72CWH1VWJCF1mmU</tool-use-id>
<output-file>/tmp/claude-1000/-home-v0id-Documents-repos-residual/0377c2d5-06de-4af8-99b6-293e1014da2c/tasks/a8317c0f57e4d6c4f.output</output-file>
<status>completed</status>
<summary>Agent "Red phase: matrix DOM interaction tests" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Confirmed: exactly 96 pre-existing tests pass unchanged, and the new suite adds 14 tests (13 failing on the intended stub throw + 1 sanity test that passes since it only exercises the already-stable `addForceRow` reducer directly).

## Summary

**Files created:**
- `/home/v0id/Documents/repos/residual/web/src/matrix-interactions.ts` — stub module exporting `MountOptions` and `mount(table, getState, setState, options?)`, which throws `new Error("not yet implemented — green phase")`. Header comment documents every design decision pinned by the tests.
- `/home/v0id/Documents/repos/residual/web/src/matrix-interactions.test.ts` — 14 tests (13 exercising `mount`, 1 sanity check of `addForceRow` alone) plus fully-implemented fixture helpers (`baseState`, `forceRowHtml`, `fixtureTableHtml`, `mountFixture`).

**`mount` signature:** `mount(table: HTMLTableElement, getState: () =&gt; PendingState, setState: (next: PendingState) =&gt; void, options?: MountOptions): void`, where `MountOptions.onChange?: () =&gt; void` fires after every state-mutating interaction, after `setState` has already run.

**Context-menu markup:** `&lt;ul role="menu"&gt;` with two `&lt;li role="menuitem" data-action="add-above|add-below"&gt;Add row above|Add row below&lt;/li&gt;` items, appended to `document.body` on `contextmenu` against a `tr.force-row`.

**New-row-starts-editing behavior pinned:** the inserted row's `.force-detail` starts *not* `hidden` and renders `&lt;input&gt;`/`&lt;select&gt;` fields directly — no `&lt;dl&gt;`, no Edit-button step — since a brand new row has nothing to view yet. New row inherits the `kind` (stressor/purpose) of the anchor row it was inserted relative to.

**Existing-row edit behavior pinned:** edits are Edit-button-only (never double-click). Edit swaps `description`/`naive change`/`outcomes` `&lt;dd&gt;`s for pre-filled `&lt;input&gt;`s and `attractor` for a `&lt;select&gt;` built from `attractorOptions(state)` (current id pre-selected), shows OK/Cancel, hides Edit. **OK** unconditionally calls `updateForceField` for all four fields (no per-field diffing — documented as intentional, since redundant same-value calls are harmless) and swaps back to a read-only `&lt;dl&gt;` with new values. **Cancel** discards input values without calling any reducer, restoring the original `&lt;dl&gt;`; state is asserted unchanged via `getState()`.

**Invalid marking:** `"row-invalid"` class applied to both `th.sticky-col[data-force-id]` and the `tr.force-row` for any key in `computeInvalidMarks(state).invalidForceKeys`; `invalidComponentColumns` is walked generically but is always empty today per `render-decisions.ts`.

**Verification:** `cd web &amp;&amp; nix develop .. -c bun test` → 96 pre-existing tests (import-parser ×2, model, render-decisions, actions) pass unchanged; all 13 new interactive tests fail cleanly via the `mount` stub's thrown Error (no unrelated compile errors); the 1 sanity test (direct `addForceRow` call, no `mount`) passes. Final tail: `97 pass / 13 fail / 456 expect() calls — Ran 110 tests across 6 files`.</result>
<usage><subagent_tokens>76969</subagent_tokens><tool_uses>14</tool_uses><duration_ms>201456</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a9ba027fee35c7454</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1000/-home-v0id-Documents-repos-residual/0377c2d5-06de-4af8-99b6-293e1014da2c/tasks/a9ba027fee35c7454.output</output-file>
<status>completed</status>
<summary>Agent "Green phase: implement matrix DOM interactions" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Test tail output confirming green:

```
 110 pass
 0 fail
 510 expect() calls
Ran 110 tests across 6 files. [229.00ms]
```

**Deviation from the plan (justified):** The header comment in the original stub file claimed "OK" unconditionally calls `updateForceField` for all four editable fields regardless of change. That contradicts the actual test `clicking OK calls updateForceField for the edited fields and swaps back to a read-only dl showing the new values`, which asserts `getState()` equals a state produced by a *single* `updateForceField` call for only the one field the test edited — an unconditional four-field call would add explicit (if same-valued) entries for `attractorId`, `naiveChangeOrFeature`, and `outcomes` to `updatedForces["S-01"]`, breaking `toEqual`. I implemented diff-based field updates (only call `updateForceField` when the input/select value differs from the value captured at Edit-time) and corrected the header comment to document the actual, tested behavior instead of the stale draft claim. No test file, `actions.ts`, `render-decisions.ts`, or `model.ts` changes were made.

Everything else (double-click cell toggle, context menu add-above/add-below with inherited kind, new-row edit-mode markup with no `&lt;dl&gt;`, Edit/OK/Cancel flow, invalid-row marking after every mutation) follows the plan as given, implemented via delegated `dblclick`/`contextmenu` listeners on the `&lt;table&gt;` plus per-element listeners on dynamically created menu items and edit controls.</result>
<usage><subagent_tokens>85091</subagent_tokens><tool_uses>14</tool_uses><duration_ms>225928</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>aa873a92c16b2ce07</task-id>
<tool-use-id>toolu_01PMbFNYYNMPX1BPtRoqTa75</tool-use-id>
<output-file>/tmp/claude-1000/-home-v0id-Documents-repos-residual/0377c2d5-06de-4af8-99b6-293e1014da2c/tasks/aa873a92c16b2ce07.output</output-file>
<status>completed</status>
<summary>Agent "Red phase: below-table forms and visibility toggle tests" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Red-phase complete. Summary:

**Files created:**
- `/home/v0id/Documents/repos/residual/web/src/forms.ts` — stub module, three exports, each throws `new Error("not yet implemented — green phase")`:
  - `regenerateStagedCommands(container: HTMLElement, getState: () =&gt; PendingState): void`
  - `emptyPendingState(base: PendingState): PendingState`
  - `mountForms(container: HTMLElement, getState: () =&gt; PendingState, setState: (next: PendingState) =&gt; void, options?: MountFormsOptions): void` where `MountFormsOptions = { onChange?: () =&gt; void }`
- `/home/v0id/Documents/repos/residual/web/src/forms.test.ts` — 17 new tests (plus 1 passing "delegation sanity" test that only exercises existing stable reducers).

**Design decisions pinned by the tests** (documented in the test file header):
- Synthetic attractor id: `NEW-ATTR-&lt;n&gt;`, n = max existing `NEW-ATTR-(\d+)` id + 1 (mirrors `addForceRow`'s `NEW-&lt;n&gt;` tempId scheme) — confirmed by reading `web/generated/cli-schema.json`'s `"add attractor"` entry, which has no `id` flag (only `description`, `name`, `negative-state`, `positive-state`, all required).
- Duplicate-component-name error UX: fixture gains a `&lt;p data-component-form-error&gt;&lt;/p&gt;` sibling to the component form; on `addComponentColumn`'s throw, `mountForms` must catch it, set that element's `.textContent` to a non-empty message, set `aria-invalid="true"` on the `name` input, and NOT call `setState` or touch the table. A subsequent successful submit must clear both.
- Component-column insertion: new `&lt;th class="sticky-row" data-component="..."&gt;` in `thead`, one `&lt;td data-residue-cell data-coupled="0" data-force-id=... data-component="..."&gt;` per existing `tr.force-row` in `tbody`, and `&lt;td data-col-total="0" data-component="..."&gt;` in `tfoot`.
- Visibility toggles hide/show via `hidden` (checked as `hasAttribute("hidden") || .hidden`) on every `th`/`td` in `thead`/`tbody`/`tfoot` matching `data-component`, driven by `visibleComponents(state, { showProposed, showUnrelated, filteredForceIds: null })`.
- `#clear-staged` resets to `emptyPendingState(base)` (keeps all `base*`, empties all `added*`/`updated*`) and clears the textarea; `#copy-staged` is tested only for "doesn't throw," not actual clipboard writes.

**Test names:** grouped under `emptyPendingState`, `regenerateStagedCommands`, `mountForms — stressor/purpose form submit` (4 tests), `mountForms — attractor form submit` (2), `mountForms — component form submit` (3), `mountForms — visibility toggles` (4), `mountForms — clear/copy staged buttons` (2), `delegation sanity` (1, passes).

**`bun test` tail:**
```
111 pass
17 fail
513 expect() calls
Ran 128 tests across 7 files. [382.00ms]
```
All 17 failures are `error: not yet implemented — green phase` thrown from `forms.ts`'s stub functions (confirmed in output) — no assertion failures, no unrelated errors. The pre-existing 110 tests (import-parser ×2, model, render-decisions, actions, matrix-interactions) plus the new passing "delegation sanity" test account for the 111 passes; nothing pre-existing regressed.

Note: `bunx tsc --noEmit` reports `Cannot find module 'bun:test'` and an `id`-missing-on-`AddedForce` literal error in `forms.test.ts` — but the identical `id`-missing pattern already exists in the pre-existing `actions.test.ts` (line 44), and the `bun:test` module-resolution complaint hits every test file including pre-existing ones equally. This is a standalone-`tsc`/bun-runtime type-resolution mismatch already present in the repo, not a regression from this change; the project's actual verification path is `bun test`, which passed/failed exactly as intended.</result>
<usage><subagent_tokens>79718</subagent_tokens><tool_uses>17</tool_uses><duration_ms>154722</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a7991b1d5b6c4581d</task-id>
<tool-use-id>toolu_011YeR3p3Znahxh9n1d4ZDmG</tool-use-id>
<output-file>/tmp/claude-1000/-home-v0id-Documents-repos-residual/0377c2d5-06de-4af8-99b6-293e1014da2c/tasks/a7991b1d5b6c4581d.output</output-file>
<status>completed</status>
<summary>Agent "Green phase: implement forms and visibility wiring" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>These are all pre-existing errors unrelated to my `forms.ts` changes (missing `bun:test` types config, a pre-existing `AddedForce`/`id` mismatch in `actions.ts`/test fixtures that predates this task, and a `.ts` import-extension config issue in `render-decisions.ts`). None involve `forms.ts`, confirming my implementation introduced no new type errors — this project apparently doesn't run `tsc --noEmit` as its gate (tests are run via `bun test` directly). No changes needed there.

## Summary

Implemented all three stub functions in `/home/v0id/Documents/repos/residual/web/src/forms.ts`:

- **`emptyPendingState(base)`** — shallow-copies `base*` arrays, resets all `added*`/`updated*` collections.
- **`regenerateStagedCommands(container, getState)`** — finds `#staged-commands`, joins `toCommandLines()` output with `\n`, prefixing invalid lines with `"# "`.
- **`mountForms(container, getState, setState, options?)`** — wires the stressor/purpose/attractor/component forms (delegating to `addForceRow`/`updateForceField`/`addAttractorOption`/`addComponentColumn`), the two visibility-toggle checkboxes (via `visibleComponents`), `#clear-staged`, and `#copy-staged` (guarded on `navigator.clipboard?.writeText`). Component-form duplicate-name errors are caught and surfaced via `[data-component-form-error]` + `aria-invalid="true"` on the name input, without touching state or the table; a later successful submit clears both.

Test result: `cd web &amp;&amp; nix develop .. -c bun test` → **128 pass, 0 fail** (up from 111 pass / 17 fail), 590 `expect()` calls across 7 files. No pre-existing test files or stable modules (`model.ts`, `actions.ts`, `render-decisions.ts`) were modified.

No deviations from the task spec were needed — the fixture's exact selectors (`table.matrix`, `th.sticky-col-right` / `td.sticky-col-right` corner cells, `[data-component-form-error]`) matched the described design directly, so column insertion, error handling, and visibility toggling all followed the spec as written.</result>
<usage><subagent_tokens>68129</subagent_tokens><tool_uses>9</tool_uses><duration_ms>71456</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a4984b904a94db160</task-id>
<tool-use-id>toolu_01QkmQESPBQNpwNR7kQwcRKV</tool-use-id>
<output-file>/tmp/claude-1000/-home-v0id-Documents-repos-residual/0377c2d5-06de-4af8-99b6-293e1014da2c/tasks/a4984b904a94db160.output</output-file>
<status>completed</status>
<summary>Agent "Red phase: import modal DOM tests" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Red phase complete.

**Files created:**
- `/home/v0id/Documents/repos/residual/web/src/import-merge.ts` — Part A stub
- `/home/v0id/Documents/repos/residual/web/src/import-merge.test.ts` — 13 tests
- `/home/v0id/Documents/repos/residual/web/src/import-modal.ts` — Part B stub
- `/home/v0id/Documents/repos/residual/web/src/import-modal.test.ts` — 6 tests

**`mergeImportedItems` signature:**
```ts
export interface MergeResult {
  state: PendingState;
  unmergeable: PendingItem[];   // parsed OK but no reducer support yet
}
export function mergeImportedItems(state: PendingState, items: PendingItem[]): MergeResult
```
`update attractor/component/term/persona` items have no field-update reducer in `actions.ts` today, so they're collected into `unmergeable` rather than silently dropped or crashed on (tested explicitly, including "mergeable items in the same batch still apply"). `add`/`update` stressor/purpose map to `updateForceField` via a `FORCE_FIELD_MAP` (`description`, `attractor-id`→`attractorId`, `naive-change`→`naiveChangeOrFeature`, `outcomes`, `shortname`; ground truth for flag names is the real `cli-schema.json`, where both stressor and purpose use `naive-change`, not a separate `feature` flag). `add component`/`add attractor` map onto `addComponentColumn`/`addAttractorOption`, with attractor ids synthesized as `NEW-ATTR-&lt;n&gt;` (own reimplementation of forms.ts's unexported scheme, since forms.ts is off-limits).

**Toggle-guard design:** `applyComponentToggles` reads the force's *effective* current component list (added-force's list, or base force merged with any pending update) via `currentComponentsFor`, and only calls `toggleComponent` when the requested add/remove would actually change membership — pinned by two tests (`add-component` on an already-coupled component is a no-op; `remove-component` on a not-coupled one is a no-op), preventing a naive unconditional toggle from inverting user intent.

**Modal markup contract** (documented in `import-modal.ts`'s header comment): `[data-import-trigger]` button, `[data-import-modal]` (toggles `hidden`), `[data-import-primary]`/`[data-import-errors]` textareas, `[data-import-error-list]` `&lt;ul&gt;` for human-readable messages, `[data-import-run]`/`[data-import-retry]` buttons. Pinned design choices: primary textarea is cleared after any successful/partial import; Retry re-parses the combined primary+errors content.

**Verification:** `cd web &amp;&amp; nix develop .. -c bun test` → `128 pass`, `19 fail`, all 19 failures are the intended `Error: not yet implemented — green phase` throws (no assertion-shape or type errors), confirming pre-existing suite is untouched and new tests are correctly red.</result>
<usage><subagent_tokens>79619</subagent_tokens><tool_uses>19</tool_uses><duration_ms>201586</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a488bcc01510c032c</task-id>
<tool-use-id>toolu_01Ntgxhp5G1QGfq4H4Yo3xcL</tool-use-id>
<output-file>/tmp/claude-1000/-home-v0id-Documents-repos-residual/0377c2d5-06de-4af8-99b6-293e1014da2c/tasks/a488bcc01510c032c.output</output-file>
<status>completed</status>
<summary>Agent "Green phase: implement import merge and modal" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>All 147 tests pass (0 fail), the pre-existing 128 remain green plus the 19 new ones (13 in import-merge.test.ts, 6 in import-modal.test.ts).

## import-merge.ts (full new logic, appended after the existing helpers)

```ts
function applyAddForce(state: PendingState, kind: "stressor" | "purpose", item: PendingItem): PendingState {
  const { state: withRow, tempId } = addForceRow(state, kind);
  let next = withRow;
  for (const [flagName, value] of Object.entries(item.fields)) {
    const field = FORCE_FIELD_MAP[flagName];
    if (field === undefined) continue;
    next = updateForceField(next, tempId, field, value);
  }
  next = applyComponentToggles(next, tempId, item);
  return next;
}

function applyAddComponent(state: PendingState, item: PendingItem): PendingState {
  return addComponentColumn(state, {
    name: item.fields.name ?? "",
    description: item.fields.description ?? "",
    status: (item.fields.status as "proposed" | "actual") ?? "proposed",
    architectureSet: item.fields["architecture-set"] ?? "",
  });
}

function applyAddAttractor(state: PendingState, item: PendingItem): PendingState {
  const id = nextAttractorId(state);
  return addAttractorOption(state, {
    id,
    name: item.fields.name ?? "",
    description: item.fields.description ?? "",
    positiveState: item.fields["positive-state"] ?? "",
    negativeState: item.fields["negative-state"] ?? "",
  });
}

function applyUpdateForce(state: PendingState, item: PendingItem): PendingState {
  const forceKey = item.fields["force-id"];
  if (forceKey === undefined) return state;
  let next = state;
  for (const [flagName, value] of Object.entries(item.fields)) {
    if (flagName === "force-id") continue;
    const field = FORCE_FIELD_MAP[flagName];
    if (field === undefined) continue;
    next = updateForceField(next, forceKey, field, value);
  }
  next = applyComponentToggles(next, forceKey, item);
  return next;
}

function isMergeable(item: PendingItem): boolean {
  if (item.kind === "add") {
    return item.type === "stressor" || item.type === "purpose" || item.type === "component" || item.type === "attractor";
  }
  return item.type === "stressor" || item.type === "purpose";
}

function applyItem(state: PendingState, item: PendingItem): PendingState {
  if (item.kind === "add") {
    if (item.type === "stressor" || item.type === "purpose") return applyAddForce(state, item.type, item);
    if (item.type === "component") return applyAddComponent(state, item);
    if (item.type === "attractor") return applyAddAttractor(state, item);
  }
  return applyUpdateForce(state, item);
}

export function mergeImportedItems(state: PendingState, items: PendingItem[]): MergeResult {
  let next = state;
  const unmergeable: PendingItem[] = [];
  for (const item of items) {
    if (!isMergeable(item)) { unmergeable.push(item); continue; }
    next = applyItem(next, item);
  }
  return { state: next, unmergeable };
}
```

## import-modal.ts (full mountImportModal + helpers)

```ts
interface ImportErrorEntry { line: string; message: string; }

function requireElement&lt;T extends Element&gt;(container: HTMLElement, selector: string): T {
  const el = container.querySelector(selector);
  if (el === null) throw new Error(`mountImportModal: missing required element ${selector}`);
  return el as T;
}

export function mountImportModal(
  container: HTMLElement,
  getState: () =&gt; PendingState,
  setState: (next: PendingState) =&gt; void,
  options?: MountImportModalOptions,
): void {
  const trigger = requireElement&lt;HTMLElement&gt;(container, "[data-import-trigger]");
  const modal = requireElement&lt;HTMLElement&gt;(container, "[data-import-modal]");
  const primary = requireElement&lt;HTMLTextAreaElement&gt;(container, "[data-import-primary]");
  const errorsBox = requireElement&lt;HTMLTextAreaElement&gt;(container, "[data-import-errors]");
  const errorList = requireElement&lt;HTMLElement&gt;(container, "[data-import-error-list]");
  const runButton = requireElement&lt;HTMLElement&gt;(container, "[data-import-run]");
  const retryButton = requireElement&lt;HTMLElement&gt;(container, "[data-import-retry]");

  const renderErrors = (entries: ImportErrorEntry[]): void =&gt; {
    errorsBox.value = entries.map((e) =&gt; e.line).join("\n");
    while (errorList.firstChild) errorList.removeChild(errorList.firstChild);
    for (const entry of entries) {
      const li = document.createElement("li");
      li.textContent = `${entry.line}: ${entry.message}`;
      errorList.appendChild(li);
    }
  };

  const processImportText = (text: string): ImportErrorEntry[] =&gt; {
    const parsed = parseImportText(text);
    const mergeResult = mergeImportedItems(getState(), parsed.items);
    setState(mergeResult.state);
    return [
      ...parsed.errors.map((e): ImportErrorEntry =&gt; ({ line: e.line, message: e.message })),
      ...mergeResult.unmergeable.map((item): ImportErrorEntry =&gt; ({
        line: item.raw,
        message: `no support yet for "${item.kind} ${item.type}" updates`,
      })),
    ];
  };

  trigger.addEventListener("click", () =&gt; modal.removeAttribute("hidden"));

  runButton.addEventListener("click", () =&gt; {
    renderErrors(processImportText(primary.value));
    primary.value = "";
    options?.onChange?.();
  });

  retryButton.addEventListener("click", () =&gt; {
    renderErrors(processImportText(`${primary.value}\n${errorsBox.value}`));
    primary.value = "";
    options?.onChange?.();
  });
}
```

## Test tail

```
src/import-modal.test.ts:
(pass) mountImportModal — opening &gt; clicking the trigger button reveals the modal
(pass) mountImportModal — successful import &gt; an all-valid paste merges into state and clears the primary textarea
(pass) mountImportModal — successful import &gt; import is additive
(pass) mountImportModal — mixed valid/invalid import &gt; splits valid and invalid lines
(pass) mountImportModal — retry &gt; fixing a previously-bad line ... merges it and clears it from the errors box
(pass) mountImportModal — retry &gt; retry leaves a still-invalid line in the errors box while merging a fixed one

 147 pass
 0 fail
 642 expect() calls
Ran 147 tests across 9 files. [352.00ms]
```

## Deviations / judgment calls (none forced by ambiguity in stubs, all consistent with documented design)

- `unmergeable` items (`update attractor/component/term/persona`) are surfaced through the same errors textarea + error-list as parse errors, using their `item.raw` text and a synthetic message — the stub comment said "re-show as an error," and no test exercises this path, so this is the simplest reading consistent with the design notes.
- Primary textarea is unconditionally cleared after both Run and Retry (not conditioned on "at least one success") — simplest reading of "clears on fully- or partially-successful import" that stays internally consistent (failed lines already relocated to the errors box, so clearing primary never loses data).
- Retry recombines `primary.value + "\n" + errorsBox.value` and reprocesses via the same `processImportText` path as Run — matches both retry tests' before/after assertions exactly (fixed line merges and clears from errors; still-bad line remains in errors, no duplicate merge).

No stable module (`import-parser.ts`, `actions.ts`, `model.ts`) needed changes — no bugs found there.</result>
<usage><subagent_tokens>60769</subagent_tokens><tool_uses>11</tool_uses><duration_ms>86763</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>afae4b2985d8130aa</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1000/-home-v0id-Documents-repos-residual/0377c2d5-06de-4af8-99b6-293e1014da2c/tasks/afae4b2985d8130aa.output</output-file>
<status>completed</status>
<summary>Agent "Red phase: bash script export tests" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Confirmed: failures are all the intended stub throw, not assertion mismatches.

## Report

Files created:
- `/home/v0id/Documents/repos/residual/web/src/export-script.ts`
- `/home/v0id/Documents/repos/residual/web/src/export-script.test.ts`

Signatures (both stubs throw `new Error("not yet implemented — green phase")`):
```ts
export function buildBashScript(state: PendingState): string
export function mountExportScript(
  container: HTMLElement,
  getState: () =&gt; PendingState,
  options?: MountExportScriptOptions, // { onChange?: () =&gt; void; triggerDownload?: TriggerDownload }
): void
export type TriggerDownload = (filename: string, content: string) =&gt; void
export const EXPORT_SCRIPT_FILENAME = "residual-import.sh"
```

Pinned exact script format (empty state):
```
#!/bin/env bash

residual write authorize

```
(header lines joined with `\n`, command lines appended, single trailing `\n`; for non-empty states it's `#!/bin/env bash\n\nresidual write authorize\n\n&lt;joined toCommandLines lines&gt;\n`).

Design decisions documented in the module header: filename `residual-import.sh`; selector `[data-generate-script]`; `residual write authorize` emitted bare (no `--minutes`, default 30 confirmed in `src/storage/write_authorization.rs:11`); download mechanics isolated behind `options.triggerDownload`, with the real default (`Blob` + `URL.createObjectURL` + hidden `&lt;a download&gt;`) guarded by `typeof URL.createObjectURL === "function"` so it no-ops instead of throwing in environments lacking support.

`bun test` tail:
```
147 pass
11 fail
643 expect() calls
Ran 158 tests across 10 files.
```
All 11 failures are `export-script.test.ts` tests failing via the stub's thrown `not yet implemented — green phase` error (confirmed via stack traces); all 147 pre-existing tests across the other 9 files pass unchanged.</result>
<usage><subagent_tokens>50802</subagent_tokens><tool_uses>12</tool_uses><duration_ms>16750240</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a9a0b81792ee3c25b</task-id>
<tool-use-id>toolu_01V6bT16i6zrPtxuJnYvqYSm</tool-use-id>
<output-file>/tmp/claude-1000/-home-v0id-Documents-repos-residual/0377c2d5-06de-4af8-99b6-293e1014da2c/tasks/a9a0b81792ee3c25b.output</output-file>
<status>completed</status>
<summary>Agent "Green phase: implement bash script export" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Full `web/src/export-script.ts` implementation (only `buildBashScript`/`mountExportScript` bodies changed; rest of file untouched):

```ts
export function buildBashScript(state: PendingState): string {
  const commandLines = toCommandLines(state).map((cl) =&gt; (cl.valid ? cl.line : `# ${cl.line}`));
  const commandSection = commandLines.length &gt; 0 ? `${commandLines.join("\n")}\n` : "";
  return `#!/bin/env bash\n\nresidual write authorize\n\n${commandSection}`;
}

// ... TriggerDownload / defaultTriggerDownload unchanged ...

export function mountExportScript(
  container: HTMLElement,
  getState: () =&gt; PendingState,
  options?: MountExportScriptOptions,
): void {
  const button = container.querySelector&lt;HTMLButtonElement&gt;("[data-generate-script]");
  if (!button) return;

  const triggerDownload = options?.triggerDownload ?? defaultTriggerDownload;

  button.addEventListener("click", () =&gt; {
    const script = buildBashScript(getState());
    triggerDownload(EXPORT_SCRIPT_FILENAME, script);
    options?.onChange?.();
  });
}
```

Test tail output:

```
 158 pass
 0 fail
 663 expect() calls
Ran 158 tests across 10 files. [382.00ms]
```

Deviations (both in the test file, not stable code):

1. `buildBashScript` conditionally omits the command section when `toCommandLines` returns zero lines, rather than always appending `join("\n") + "\n"`. This is required so the "truly empty state" pinned test (`"#!/bin/env bash\n\nresidual write authorize\n\n"`, no third `\n`) passes.

2. That conditional broke two pre-existing test cases that used base-only (`baseAttractors`/`baseForces` with no `updatedAttractors`/`updatedForces`) fixtures under the mistaken assumption that base entries alone produce command lines. Per `model.ts`'s documented, intentional design (`toCommandLines`/`orderedEntries`), only added/updated entries ever emit a line — base entries with no diff emit nothing. I verified this directly against `model.ts` (confirmed `toCommandLines` returns `[]` for base-only state). Given that ground truth, the "two-line state" test's own dynamically-computed `expected` (built from `toCommandLines`) and the hardcoded "truly empty state" `expected` are mathematically incompatible for any pure implementation of `buildBashScript`, since both fixtures produced identical (empty) `commandLines`. I fixed this by adding no-op `updatedAttractors: { "A-01": {} }` / `updatedForces: { "S-01": {} }` to those two test states (in `export-script.test.ts`, the "pins the exact full script format for a simple two-line state" test and the "reflects the latest getState()" test) so they actually exercise two real, distinct command lines as their names imply, rather than modifying `model.ts`'s intentional add/update-only semantics or the pinned-format assertions.

Files touched: `/home/v0id/Documents/repos/residual/web/src/export-script.ts` (implementation), `/home/v0id/Documents/repos/residual/web/src/export-script.test.ts` (two fixture fixes, explained above; no assertions changed).</result>
<usage><subagent_tokens>50887</subagent_tokens><tool_uses>25</tool_uses><duration_ms>198842</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a758f57ef0c4ad466</task-id>
<tool-use-id>toolu_01UMpRniAwXkfTid7FowHwUr</tool-use-id>
<output-file>/tmp/claude-1000/-home-v0id-Documents-repos-residual/0377c2d5-06de-4af8-99b6-293e1014da2c/tasks/a758f57ef0c4ad466.output</output-file>
<status>completed</status>
<summary>Agent "Red phase: snapshot conversion tests" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Confirmed — failures are all the intended stub throw, not import/type errors.

## Report

**Files created:**
- `/home/v0id/Documents/repos/residual/web/src/snapshot-bridge.ts`
- `/home/v0id/Documents/repos/residual/web/src/snapshot-bridge.test.ts`

**Types and signature** (in `snapshot-bridge.ts`):
```ts
export interface RawSnapshotForce { id: string; shortname: string; description: string; naive_change: string; outcomes: string; attractor_id: string; kind: "stressor" | "purpose"; source: "sidecar" | "working"; }
export interface RawSnapshotAttractor { id: string; name: string; description: string; positive_state: string; negative_state: string; source: "sidecar" | "working"; }
export interface RawSnapshotComponent { name: string; description: string; status: string; architecture_set: string; source: "sidecar" | "working"; }
export interface RawSnapshotResidue { force_id: string; component_id: string; coupled: boolean; whole_system: boolean; notes: string; source: "sidecar" | "working"; }
export interface RawLandscapeSnapshot { attractors: RawSnapshotAttractor[]; stressors: RawSnapshotForce[]; purposes: RawSnapshotForce[]; components: RawSnapshotComponent[]; residues: RawSnapshotResidue[]; }
export function snapshotToPendingState(raw: RawLandscapeSnapshot): PendingState
```
`PendingState`/`SnapshotAttractor`/`SnapshotComponent`/`SnapshotForce` imported (type-only) from `./model`, not redefined. Stub throws `new Error("not yet implemented — green phase")`.

**Test names** (`describe("snapshotToPendingState")`):
1. maps a single attractor field-by-field to camelCase
2. maps a single component field-by-field to camelCase
3. maps a stressor from raw.stressors to kind 'stressor' with renamed fields
4. maps a purpose from raw.purposes to kind 'purpose' regardless of its own raw kind field
5. computes a force's components from coupled residues, excluding uncoupled and other-force rows
6. orders baseForces as mapped stressors first, then mapped purposes
7. all added* collections start empty and updated* records start empty
8. baseTerms and basePersonas start empty since raw snapshot carries no term/persona data
9. an empty raw snapshot yields a valid, fully-empty PendingState with no crash

Confirmed the `stressors.chain(purposes)` ordering against `src/view/components/matrix.rs:123-126` before writing the ordering test, and confirmed `PendingState`'s exact field list against `web/src/model.ts` and the `emptyState` helper in `web/src/model.test.ts`.

**`bun test` tail output:**
```
9 tests failed:
(fail) snapshotToPendingState &gt; maps a single attractor field-by-field to camelCase
(fail) snapshotToPendingState &gt; maps a single component field-by-field to camelCase
(fail) snapshotToPendingState &gt; maps a stressor from raw.stressors to kind 'stressor' with renamed fields
(fail) snapshotToPendingState &gt; maps a purpose from raw.purposes to kind 'purpose' regardless of its own raw kind field
(fail) snapshotToPendingState &gt; computes a force's components from coupled residues, excluding uncoupled and other-force rows
(fail) snapshotToPendingState &gt; orders baseForces as mapped stressors first, then mapped purposes
(fail) snapshotToPendingState &gt; all added* collections start empty and updated* records start empty
(fail) snapshotToPendingState &gt; baseTerms and basePersonas start empty since raw snapshot carries no term/persona data
(fail) snapshotToPendingState &gt; an empty raw snapshot yields a valid, fully-empty PendingState with no crash

 158 pass
 9 fail
 663 expect() calls
Ran 167 tests across 11 files. [452.00ms]
```
Verified all 9 failures are the intended `not yet implemented — green phase` throw (spot-checked via direct run), not type/import errors. All 158 pre-existing tests pass unchanged.</result>
<usage><subagent_tokens>52393</subagent_tokens><tool_uses>11</tool_uses><duration_ms>71489</duration_ms></usage>
</task-notification>