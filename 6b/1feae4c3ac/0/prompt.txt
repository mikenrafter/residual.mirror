Add the MIT LICENSE file. Then explore the entireio metadata branch to see if there's anything sensitive there that would be unwise to publish. I would also like the rg-tdd-implement skill to be installed as `residual-tdd-implement`. I would also like for the skill installer to let the operator install all skills w/ one command. Let it do so for all agents as well... `--agent=all` and `skill-install all`... Also, the hyphenated subcommands is an antipattern, I would like `skill install` and `help skill install`, etc...